name: breaking_bad_flutter
description: A Flutter app to display Breaking Bad characters.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cached_network_image: ^3.2.0 # Untuk caching gambar karakter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
